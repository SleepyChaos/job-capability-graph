# 需确认清单

> 维护规则：开发过程中遇到需要用户明确的决策，记录在此并继续开发（跳过或降级处理），不阻塞进度。用户确认后更新状态并回填到对应实现。
>
> 状态说明：`待确认` / `已确认` / `已按降级方案处理`
>
> 最后更新：2026-08-12（Q1 已确认并接入 DeepSeek；其余项仍待确认）

| # | 事项 | 背景与影响 | 当前降级处理 | 状态 |
| --- | --- | --- | --- | --- |
| Q1 | LLM Provider 与 API Key：是否提供 DeepSeek（或其他 OpenAI 兼容）API Key | 阶段 D4 LLM 网关需要真实 Key 才能演示表达优化/追问/解释；无 Key 时全部走规则降级 | 已配置 DeepSeek Key（backend/.env，已加入 .gitignore 不入库）；表达层与匹配解释均已实测走 llm_expression/llm_explanation | 已确认（2026-08-12） |
| Q2 | 真实采集数据源名单：允许采集哪些招聘页/企业官网/政府页面 | 阶段 D3 要求至少 1 个真实来源；合规边界需项目负责人确认（robots、频率、用途） | 先实现采集框架与手工触发，来源注册留空待批 | 待确认 |
| Q3 | 评测标注人力：120 条 JD 双人标注与 300 对匹配三分类由谁执行 | 阶段 E 金标准需要人工标注；人力不足将影响三项 90% 指标口径 | 开发侧先提供标注工具与候选清单，支持单人标注降级口径 | 待确认 |
| Q4 | 匹配准确率金标准是否采纳"三分类 Accuracy"为官方主指标 | 《测试数据集设计方案》§7.1 建议，需团队确认 | 按方案建议实现评测脚本，指标口径可在报告中调整 | 待确认 |
| Q5 | 新岗位演示样例命名：是否采用"机器人世界模型数据工程师" | 阶段 F3 场景包需要固定演示主题 | 场景包结构先建，名称以真实推演结果为准 | 待确认 |
| Q6 | 部署环境：MySQL 8 实例由谁提供（本地 Docker / 云实例） | 阶段 F1/F2 需要 MySQL 验证迁移 | 先用 Docker Compose 本地 MySQL 8 验证 | 待确认 |
| Q7 | 认证体系范围：最小 JWT（三角色）是否满足，还是需要对接外部账号系统 | 阶段 D5 | 按最小 JWT 实现 | 待确认 |
| Q8 | 审核人身份：现有 `X-Reviewer-Code` 机制是否保留或全部迁移为 JWT | 阶段 D5 改造审核接口 | JWT 上线后统一迁移，旧头废弃 | 待确认 |
| Q9 | 简历数据保存期限与删除策略默认值 | 阶段 D6 隐私操作 | 默认实现"用户请求即软删 + 派生结果失效标记"，期限字段留配置 | 待确认 |
| Q10 | 病毒扫描：是否接入真实扫描引擎（如 ClamAV） | 阶段 D2 文件安全 | 首期实现扫描接口预留 + 规则检查（大小/类型/扩展名），不接真实引擎 | 待确认 |
| Q11 | 存量 3718 条 JD 是否重跑解析以回填场景表 | 解析运行幂等锁定，重跑需重置运行记录并耗时数分钟 | 场景抽取仅对新增解析生效；存量不回填（降级清单 D12） | 待确认 |
| Q12 | Docker/MySQL 验证环境：本机是否可用 Docker 运行 compose 验证 MySQL 方言 | docker-compose.yml/Dockerfile/CI 已就绪，但本机未验证镜像构建与 MySQL 迁移 | 交付前需在有 Docker 的环境执行一次 `docker compose up --build` 全流程验证 | 待确认 |
| Q13 | 演示视频录制与旁白由谁完成 | 演示脚本与两条闭环已可重复执行（场景包就位） | 开发侧提供可重复演示步骤，视频录制待人工 | 待确认 |
