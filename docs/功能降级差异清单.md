# 功能降级差异清单

> 维护规则：开发中遇到难以实现、外部依赖缺失或时间不允许的功能，记录降级方案与设计原文的差异，保证交付文档可解释。用户批准恢复时再升级实现。
>
> 状态说明：`降级生效` / `已恢复完整实现` / `待评估`
>
> 最后更新：2026-08-12（Q1 接入 DeepSeek，D13 部分恢复；其余降级项不变）

| # | 功能 | 设计原文要求 | 降级实现 | 差异影响 | 恢复条件 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| D1 | 异步任务 Worker/Redis（G17） | Redis 队列 + Worker 异步执行采集/OCR/聚类/匹配 | 同步任务执行 + tools CLI 触发；任务运行记录表保留可回放字段 | 长任务阻塞请求；不影响数据正确性 | 部署资源到位且性能测试显示必要 | 降级生效 |
| D2 | Outbox 事件（G17） | 事务提交后 Outbox 驱动缓存/索引刷新 | 同事务内直接刷新投影/统计；图谱查询实时计算 | 极端并发下最终一致性弱于 Outbox | 引入真实队列时补齐 | 降级生效 |
| D3 | Leiden 社区发现（G20） | 数据充分时运行 Leiden 社区发现 | 恒走规则分组回退（technology_cooccurrence_fallback），fallback_reason 入库 | 任务社区凝聚度指标为规则口径 | 任务/证据规模达到阈值且引入图算法库 | 降级生效 |
| D4 | SQLite → MySQL（G3 部分） | MySQL 8 生产库 | 开发期 SQLite；部署阶段做方言验证 | 部分 MySQL 特性（生成列约束等）需在验证时核对 | 阶段 F2 | 待评估 |
| D5 | 对象存储 | S3 兼容对象存储 | 本地文件路径 + storage_object_key 字段保留 | 多机部署需改造 | 部署阶段按需 | 降级生效 |
| D6 | 图谱渲染引擎（G19） | AntV G6 / ECharts | 自绘 div/CSS/SVG，提供表格降级视图 | 大规模图（>500 节点）性能与交互受限 | 阶段 F7 评估后决定 | 待评估 |
| D7 | 顶栏全局时间/T 领域筛选 | 全局筛选生效于所有页面 | 按钮禁用并标注"待接入"；全局搜索已接数据管理中心 | 跨页筛选暂不可用 | 阶段 C 后评估 | 降级生效 |
| D8 | 数据管理中心导出/新建/JD 编辑/原始文档 tab | 完整的编辑与版本管理 | 按钮禁用标注；原始文档查询等待阶段 D 采集适配器 | 仅查询能力，写操作未开放 | 阶段 D | 降级生效 |
| D9 | 技术词候选词创建 | 候选词池登记与审核 | 按钮标注"待接入（阶段 E 治理流程）" | 候选词治理未开放 | 阶段 E | 降级生效 |
| D10 | 系统设置入口 | LLM/调度/参数设置页 | 侧边栏入口禁用，等待阶段 D 认证上线 | 设置能力缺失 | 阶段 D | 降级生效 |
| D11 | 热力图 L1/L3 口径落库 | 按日触发指标全口径可回放 | 仅 L2 口径落库（stored_daily_metric），L1/L3 运行时投影（runtime_projection） | L1/L3 口径不可离线复核 | 后续按需扩展落库层级 | 降级生效 |
| D12 | 场景抽取存量回填 | rel_job_scenario 覆盖全部 JD | 存量演示库解析运行已幂等锁定，场景抽取仅对新解析运行生效 | 存量 3718 条 JD 无场景行 | 需确认是否重置解析运行重跑（见需确认清单 Q11） | 降级生效 |
| D13 | 扫描件 OCR 与追问 LLM 化 | 扫描 PDF/图片 OCR；LLM 动态追问 | LLM 已接入 DeepSeek（表达层/匹配解释实测生效，Q1 已确认）；追问仍用确定性题库选题；扫描件明确报错提示转文本 | OCR 能力缺失，追问未用 LLM 改写 | OCR 引擎接入；追问 LLM 化按需评估 | 降级生效（部分恢复） |
| D14 | 采集调度与异步执行 | Worker 定时调度 | 采集在 HTTP 请求内同步执行，入口页→详情页一层深入、限速与增量哈希已实现 | 大批量采集会阻塞请求 | Worker 接入（阶段 F 评估） | 降级生效 |
| D15 | 官方三项 90% 指标 | 专家金标准冻结后正式评测 | JD 解析与匹配指标如实报告 pending_gold；简历解析以 30 份合成集自检（91.62，非官方口径） | 官方指标暂无正式数字 | Q3/Q4 专家标注完成 | 降级生效 |
| D16 | Docker/MySQL 实机验证 | compose 全流程跑通 | Dockerfile/compose/CI 已就绪，本机未验证镜像构建与 MySQL 迁移 | 部署可行性未经实机确认 | Q12 有 Docker 环境验证 | 待评估 |

> 后续开发中新增的降级项继续追加到本表。
