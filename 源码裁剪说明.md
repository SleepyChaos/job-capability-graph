# 源码裁剪说明

本项目向赛事提交的源码包（`交付材料-源代码/`）是**经过裁剪的版本**。本文件逐条说明
裁掉了什么、为什么裁、保留了什么，以及评审需要完整实现时如何获取。

裁剪是主动声明的，不是隐去后当作完整交付。被裁的位置全部保留了目录、文件名与公开
接口签名，评审可以看清「这里有什么、被裁了什么」。

## 一、为什么裁剪

新岗位发现推演、岗位聚类、结构化抽取与离线数据管线是本项目的核心方法，也是参赛作品
的主要创新点。以可直接复用的源码形态公开分发，风险超出赛事评审的必要范围。

因此采取的方式是：**完整实现保留在私有仓库，可按赛事要求向评审开放访问；公开提交的
源码包裁掉这些模块的实现，保留其余全部代码。**

## 二、裁剪范围

| 范围 | 文件数 | 原始行数 | 内容 |
| --- | ---: | ---: | --- |
| `backend/tools/` | 82 | 18,469 | 离线管线：采集、抽取、聚类、推演、实验与评测脚本 |
| `backend/app/modules/discovery/` | 4 | 3,279 | 新岗位发现推演：候选生成、gap 分级、共现期望、传导时滞先验、前瞻计算 |
| `backend/app/modules/clustering/` | 2 | 1,898 | 岗位聚类与重分配算法 |
| `backend/app/modules/extraction/` | 5 | 835 | JD 结构化抽取、职业名归一、近重复判定、发布门禁 |
| **合计** | **93** | **24,481** | |

逐文件清单见 [`交付材料-源代码/裁剪清单.json`](交付材料-源代码/裁剪清单.json)。

### 四处例外（保留）

- **`discovery/models.py`、`clustering/models.py` 保留**。它们是纯 SQLAlchemy 表声明，
  不含算法。裁掉只会让 Alembic 迁移与接口模型失去参照，对保护核心方法没有帮助。
- **`__init__.py` 保留**，仅为包结构。
- **模块级常量保留真值**，包括 `COMPONENT_WEIGHTS`、`EVENT_TYPE_WEIGHTS`、
  `CHANNEL_WEIGHTS`、`AUTO_PUBLISH_THRESHOLD`、`MANUAL_REVIEW_THRESHOLD` 等参数。
  理由与 `models.py` 一致：常量是声明不是方法，权重脱离「如何组合」没有复用价值；
  而非裁剪代码会 import 它们，给假值等于让保留下来的功能**静默算错**，比交出参数更糟。
- **各模块原有的 `import` 语句保留**。它们声明依赖关系，不含实现；少了它们，保留的
  dataclass 字段默认值与类型注解会在导入期 `NameError`，整个后端起不来。

前两项让代码可读，后两项让包**可运行**（见第五节）。

## 三、保留了什么

| 部分 | 文件数 | 源码行数 |
| --- | ---: | ---: |
| 保留的源码合计 | 233 | 49,878 |

行数只统计源码（`.py` / `.ts` / `.md` 等），不含图谱 JSON 等数据产物。

具体包括：

- **全部前端**（`frontend/src/`，53 个 TypeScript / React 文件）——页面、图谱组件、接口客户端
- **API 路由层**（`backend/app/api/`）——所有接口的入参、出参与调用关系完整可读
- **数据层**（`backend/app/db/`、`backend/app/core/`、`backend/migrations/`）——含全部 Alembic 迁移
- **未裁剪的业务模块**——`talent`、`job`、`graph`、`organization`、`data_center`、`ingestion`、`taxonomy`
- **全部单元测试**（`backend/tests/`，38 个文件）
- **部署文件**——`docker-compose.yml`、两个 Dockerfile、`nginx.conf`

## 四、被裁的位置长什么样

实现替换为保留签名的桩，例如 `backend/app/modules/discovery/service.py`：

```python
"""
backend/app/modules/discovery/service.py（交付裁剪版）。

—— 交付裁剪声明 ——
本文件的实现已按根目录《源码裁剪说明.md》移除，仅保留公开接口签名，
以便评审确认接口边界与调用关系。完整实现保留在私有仓库，可按赛事要求授权。
"""

from __future__ import annotations

from app.core.redacted import RedactedFeatureError

ALGORITHM_VERSION = "discovery_v3"          # 常量保留真值，见第二节例外

def run_discovery(db: Session, *, mode_code: str, target_date: date, ...) -> DiscoveryResult:
    raise RedactedFeatureError("app.modules.discovery.service.run_discovery")

def auto_candidate_portrait(db: Session, *, candidate_code: str) -> dict:
    """为推演派生岗位补五维能力画像，写进标准 JD 的 content_json。"""
    raise RedactedFeatureError("app.modules.discovery.service.auto_candidate_portrait")
```

保留的信息：模块与函数的文档字符串、公开函数与类名、完整参数与返回类型注解、模块级
常量、原有 import、`@dataclass` 装饰器与字段声明、内置异常基类，以及以注释形式记录的
原装饰器与原基类。**移除的信息：函数体，即算法实现本身。**

## 五、对其他交付物的影响（重要）

### 1. 裁剪版源码包可以运行，但功能有明确边界

`交付材料-源代码/src` 内 `docker compose up -d --build` 即可启动，随包附带已审计的运行库
快照（84 MB，不含任何源码）。**已在干净卷上实测**：

```
mysql / backend / frontend    三个容器全部 healthy
后端健康检查                   {"status":"ok","database":"ok"}
无参 GET 端点                  37 条中 36 条返回 200（另 1 条为需登录的 /auth/me）
前端                          index.html 200、/api 代理 200
```

被裁的是**计算与写入**逻辑，读取链路完整。因此：

| 功能 | 裁剪版 |
| --- | --- |
| 岗位画像图谱、产业图谱、技术图谱浏览 | ✅ 完整可用，真实数据 |
| 新岗位发现候选卡、五维画像查看 | ✅ 完整可用 |
| 岗位聚类结果、技术词表、数据采集治理 | ✅ 完整可用 |
| 运行自动预测生成候选（`run_discovery`） | ⛔ HTTP 501 |
| 生成五维画像（`auto_candidate_portrait`） | ⛔ HTTP 501 |
| 重新执行岗位聚类 | ⛔ HTTP 501 |
| 从核心 XLSX 重建数据（离线管线） | ⛔ `backend/tools/` 全为桩 |

调用到被裁实现时返回 **501 Not Implemented**，响应体明确说明原因并指向在线部署——
评审据此能区分「这个功能被裁了」与「这个功能坏了」：

```json
{
  "error": "redacted_in_delivery",
  "message": "本功能的实现未包含在交付裁剪版源码包中。",
  "detail": "app.modules.discovery.service.run_discovery",
  "online_deployment": "http://122.51.220.41:8080/",
  "reference": "源码裁剪说明.md"
}
```

**完整功能请访问在线部署 <http://122.51.220.41:8080/>**，该环境运行的是完整代码。

为实现这一点，构建脚本向裁剪包注入了两处**非原始代码**，均已在文件内标注来源：
`backend/app/core/redacted.py`（占位异常）与 `app/main.py` 末尾追加的 501 处理器。
仓库中的 `app/main.py` 未被修改。

### 2. 单元测试结果基于完整代码

[`交付材料-单元测试/`](交付材料-单元测试/) 中的 **161 项通过、语句覆盖率 78.79%**，是在
**完整代码**上运行得出的，不是在裁剪版上。

38 个测试文件中有 **19 个**直接依赖被裁模块，在裁剪版源码包上执行会失败。这是裁剪的
必然结果，不是测试本身的缺陷。需要复核覆盖率数据的评审请通过下一节的途径获取完整仓库。

### 2.1 覆盖率报告中被裁模块的逐行页同样已处理

coverage.py 的 HTML 报告会**逐行内嵌被测源码**。若原样交付，被裁模块的完整实现会以
HTML 形式重新出现，浏览器打开即可逐行阅读——源码裁剪等于没做。因此 `交付材料-单元测试/coverage/`
下这 8 份逐行页已替换为说明页：

| 文件 | 原页面体积 | 语句 / 未覆盖 / 覆盖率 |
| --- | ---: | --- |
| `app/modules/discovery/service.py` | 839,036 字符 | 696 / 161 / 77% |
| `app/modules/clustering/service.py` | 428,299 字符 | 473 / 58 / 88% |
| `app/modules/clustering/algorithm.py` | 198,254 字符 | 289 / 16 / 94% |
| `app/modules/extraction/job_structure.py` | 106,339 字符 | 147 / 5 / 97% |
| `app/modules/discovery/algorithm.py` | 99,272 字符 | 123 / 3 / 98% |
| `app/modules/discovery/foresight.py` | 76,796 字符 | 91 / 10 / 89% |
| `app/modules/discovery/itemsets.py` | 59,046 字符 | 74 / 5 / 93% |
| `app/modules/extraction/near_duplicate.py` | 47,205 字符 | 78 / 2 / 97% |

**处理的是展示层，不是数据**：

- 汇总页 `index.html` 上这些文件的行**一条不少**，语句数、未覆盖数与百分比原样保留
- 全项目总计 **10,522 语句 / 2,232 未覆盖 / 78.79%** 未改动
- 替换后的页面本身列出该文件的三项数字，并保留返回汇总页的链接（不做成断链）
- `backend/tools/` 不涉及：覆盖率范围是 `source = ["app"]`，tools 本就不在报告里

处理由 [`scripts/redact_coverage_report.py`](scripts/redact_coverage_report.py) 完成，
与源码裁剪共用同一份范围配置，可复现。

### 3. 随包的数据产物 ≠ 源代码

为使裁剪包可运行，以下数据随包分发。它们是**数据管线的产出物，不含任何源代码**，
与本次裁剪无关：

| 文件 | 体积 | 缺了会怎样 |
| --- | ---: | --- |
| `src/db/job_capability_graph.sql.gz` | 84 MB | 库为空，所有页面无数据 |
| `frontend/public/job-ecosystem-graph.json` | 39.5 MB | 岗位画像图谱空白 |
| `backend/data/job_graph_bridge.json` | 3.25 MB | 人岗匹配的图谱关联全部显示「尚未关联」 |
| `frontend/public/enterprise-industry-graph.json` | 1.75 MB | 产业图谱空白 |

裁剪包体 131 MB，其中 128 MB 是上述数据，源码本身仅约 3 MB。

库快照由 [`scripts/dump_runtime_db.sh`](scripts/dump_runtime_db.sh) 从已审计的运行库导出，
90 张表，含 4,655 条岗位事实、106 个标准岗位、178 条新岗位发现候选（其中两条已入库并
带完整五维画像）。

## 六、如何获取完整实现

| 项 | 值 |
| --- | --- |
| 仓库 | <https://github.com/SleepyChaos/job-capability-graph> |
| 可见性 | 私有仓库，可按赛事要求开放评审账号权限 |
| 对应提交 | `93af896`（分支 `demo/frontend-data-alignment`） |
| 主分支 | `p2-role-discovery-and-evolution` |

完整仓库含全部 24,481 行被裁实现、完整提交历史（149 次提交，带时间戳）与全部测试。
提交历史同时作为本作品的原创性与研发过程记录。

## 七、复现本裁剪版

裁剪由脚本完成，可复现、可核对：

```bash
bash scripts/dump_runtime_db.sh                     # 导出运行库快照
python scripts/build_redacted_source_package.py     # 生成裁剪包
python scripts/redact_coverage_report.py            # 处理覆盖率报告逐行页
```

第一条在数据卷上临时起一个 MySQL 导出快照，用完即删（认卷不认容器：容器会被
Docker Desktop 清理带走，卷一直在）。

第二条以 `git ls-files` 界定源码范围（因此虚拟环境、依赖缓存、构建产物与 `.env` 一律不会
进包），对 `REDACT_DIRS` 中的文件用 AST 提取公开签名后生成桩，并输出 `裁剪清单.json`。

第三条处理覆盖率报告的逐行页（见第五节 2.1），范围配置从第二条脚本导入，两边不会漂移。
覆盖率报告本身需先按 `交付材料-单元测试/单元测试报告.md` 的命令生成。

## 八、使用声明

本源码包仅供赛事评审使用，不授予再分发、再实施或商业使用的权利。
