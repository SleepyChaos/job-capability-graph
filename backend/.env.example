# 本地后端使用：复制本文件为 `backend/.env`，再填写 DeepSeek Key。
# `backend/.env` 已被 .gitignore 排除，禁止提交真实密钥。
APP_ENV=development
APP_DATABASE_URL=sqlite+pysqlite:///./.local/dev.db
APP_LOG_LEVEL=INFO
APP_API_PREFIX=/api/v1

# DeepSeek 用于简历事实/关键词抽取和文字解释；不参与人岗匹配机械评分。
# 无 Key 或请求失败时，简历抽取自动降级为现有规则。
APP_LLM_API_KEY=sk-your-deepseek-key-here
APP_LLM_BASE_URL=https://api.deepseek.com/v1
APP_LLM_MODEL=deepseek-v4-flash
APP_LLM_TIMEOUT_SECONDS=30

# 也兼容旧变量名 OPENAI_API_KEY、OPENAI_BASE_URL、LLM_MODEL。

# 认证（D5）：生产部署必须改为随机密钥
APP_AUTH_SECRET=

# 生产/联调环境示例（不要在仓库中填写真实口令）
# APP_DATABASE_URL=mysql+pymysql://app:password@mysql:3306/job_capability_graph?charset=utf8mb4
