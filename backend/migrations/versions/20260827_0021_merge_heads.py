"""合并两条迁移分支。

`20260818_0014` 由图谱分支引入（三方矛盾审计表），从 `20260812_0013` 分出；
本仓库主线自 `20260812_0013` 起另走 `20260814_0014 … 20260823_0020`。
两条链互不相干——前者建新表，后者改候选表——因此本修订只做汇合，不含任何 DDL。
"""

from __future__ import annotations

revision: str = "20260827_0021"
down_revision: tuple[str, str] = ("20260823_0020", "20260818_0014")
branch_labels = None
depends_on = None


def upgrade() -> None:
    """两条分支各自的变更已在其修订中完成，此处无需再做。"""


def downgrade() -> None:
    """回退到分叉状态即可，不撤销任一分支的变更。"""
