# 交付材料说明

本仓库根目录下有两份独立交付物，各自完整、可单独审阅：

| 目录 | 交付物 | 对应要求 |
| --- | --- | --- |
| [`交付材料-软件模块/`](交付材料-软件模块/) | 源代码说明、部署说明、单元测试报告与证据、部署文件副本 | 赛题「软件模块」：源代码、可执行程序、部署说明、单元测试用例（覆盖率 ≥ 60%） |
| [`交付材料-新岗位发现测试链路/`](交付材料-新岗位发现测试链路/) | 新岗位发现两条外部证据链路的输入样例、输出原文、数据库样例与平台截图 | 功能验证材料：研究侧（`upstream_gap`）与产业侧（`milestone_gap`）端到端可追溯性 |

## 一、软件模块

```
交付材料-软件模块/
├── README.md              交付物清单与四项对照
├── 01_源代码说明.md        仓库地址、分支、代码规模、模块职责
├── 02_部署说明.md          Docker 启动步骤、已知坑、排障
├── 03_单元测试报告.md      测试范围、覆盖率结论与复现命令
├── 单元测试输出.txt        pytest 原始输出（归档证据）
├── coverage/              覆盖率 HTML 报告（本地生成，不入库）
└── deploy/                docker-compose.yml 与两个 Dockerfile 的副本
```

核心结论：`docker compose up -d backend frontend` 一条命令拉起全栈；后端 **161 项单元测试全部通过，语句覆盖率 78.79%**，高于要求的 60%。

## 二、新岗位发现测试链路

```
交付材料-新岗位发现测试链路/
├── 测试报告.md / .docx     主报告，含两条完整链路的输入、输出与平台呈现
├── export_test_chain.py    导出脚本，口径与判定工具一致，可复跑
├── input/                  两条链路各自的输入样例（技术词、招聘侧基线、证据源）
├── output/                 两条链路的候选完整记录（事实卡、表达层、标准 JD）
├── db/                     数据库样例 SQL（43 条候选及其证据/审核/入库记录）+ 生成脚本
└── screenshots/            平台呈现截图（数据卡、发现库、审核台、图谱、侧栏）
```

核心结论：两条链路的招聘侧基线（321/2/0 与 44/108/0）均可用库内原始数据现场复算，与候选卡记录逐项吻合。

## 打包提交

两个目录可直接连同仓库整体压缩提交。注意 `交付材料-软件模块/coverage/` 是 `pytest --cov-report=html` 的产物、不入 git（coverage.py 会在该目录内自带 `.gitignore`），从仓库克隆的副本里没有它——**打包前确认本地已生成，或按 `03_单元测试报告.md` 的命令重新生成后再压缩**。
