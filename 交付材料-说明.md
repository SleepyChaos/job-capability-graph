# 提交说明

具身智能岗位能力图谱平台 · 软件模块交付材料。

## 一、赛题交付项对照

| 赛题要求 | 本次提交 |
| --- | --- |
| 源代码 | [`交付材料-源代码/`](交付材料-源代码/) —— **裁剪版**，核心算法模块实现已移除，见下第三节与《源码裁剪说明.md》 |
| 可执行程序 | **在线部署 <http://122.51.220.41:8080/>**（完整代码）；裁剪版源码包亦可本地 `docker compose up` 运行 |
| 部署说明（Dockerfile / 容器化部署） | [`交付材料-Docker部署/`](交付材料-Docker部署/) |
| 单元测试用例（覆盖率 ≥ 60%） | [`交付材料-单元测试/`](交付材料-单元测试/) —— **161 项通过，语句覆盖率 78.79%** |

## 二、在线部署

**<http://122.51.220.41:8080/>**

系统功能已部署上线，评审可在线开放访问，**无需安装任何环境**。

该环境运行的是**完整代码**，全部功能可用，包括：

- 新岗位发现：运行自动预测生成候选卡 → 审核台逐条处置入库 → 岗位画像图谱查看呈现 → 岗位能力演变追踪
- LLM 生成候选表达层与五维能力画像
- 岗位聚类、结构化抽取、数据采集治理全链路

这是评审查看完整功能的推荐入口。

## 三、关于源码裁剪

新岗位发现推演、岗位聚类、结构化抽取与离线数据管线是本项目的核心方法，也是参赛作品的
主要创新点。

因此采取的方式是：**将系统功能部署上线到 <http://122.51.220.41:8080/>，评审可在线开放
访问；公开提交的源码包裁掉这些模块的实现，保留其余全部代码。**

裁剪是**主动声明**的，不是隐去后当作完整交付：

| | 文件数 | 源码行数 |
| --- | ---: | ---: |
| 裁剪 | 93 | 24,481 |
| 保留 | 233 | 49,878 |

被裁位置保留目录、文件名、公开接口签名、模块级常量与文档字符串，评审可以看清「这里有
什么、被裁了什么」。**完整实现保留在私有仓库，可按赛事要求向评审开放访问权限。**

逐条说明见 **[`源码裁剪说明.md`](源码裁剪说明.md)**，含裁剪范围、四处保留例外、对单元
测试结果的影响、覆盖率报告的相应处理，以及完整实现的获取途径。

## 四、三份材料

### 源代码（裁剪版，可运行）

```
交付材料-源代码/
├── README.md              功能边界、启动方式、实测结论
├── 裁剪清单.json           逐文件裁剪清单（路径 + 原始行数）
└── src/
    ├── docker-compose.yml 裁剪包专用编排
    ├── db/                运行库快照 84 MB，首启自动导入
    ├── backend/           API 路由层、数据层、迁移、全部单元测试
    └── frontend/          全部前端源码与图谱产物
```

```bash
cd 交付材料-源代码/src && docker compose up -d --build
```

首次启动约 6 分钟。已在干净卷上实测：三个容器全部 healthy，37 个无参 GET 端点中 36 个
返回 200（另一个为需登录的 `/auth/me`），前端与 `/api` 代理均正常。

**功能边界**：读取链路完整——岗位画像图谱、产业图谱、技术图谱、候选数据卡、五维画像、
聚类结果、技术词表均可正常浏览，数据真实。调用到被裁实现的操作（运行预测、生成画像、
重跑聚类）返回 **HTTP 501** 并在响应体中指向在线部署，这是有意声明而非故障。

### Docker 部署

```
交付材料-Docker部署/
├── README.md              两个启动入口与容器编排对照
├── Docker部署说明.md       环境要求、启动方式、编排细节、两个已知坑、排障表
├── 源代码说明.md           仓库地址、分支、代码规模、目录职责、技术栈
└── deploy/                docker-compose.yml + 两个 Dockerfile + nginx.conf
```

### 单元测试

```
交付材料-单元测试/
├── README.md              结论、材料索引与覆盖率门禁
├── 单元测试报告.md         测试范围、复现命令、四个挂载的必要性
├── 单元测试输出.txt        pytest 原始输出（归档证据）
└── coverage/              覆盖率 HTML 报告
```

后端 **161 项单元测试全部通过，语句覆盖率 78.79%**，高于要求的 60%。阈值写死在
`pyproject.toml` 的 `fail_under = 60`，低于该线直接判失败，不依赖人工检查。

⚠️ 这两个数字是在**完整代码**上跑出来的。38 个测试文件中有 19 个依赖被裁模块，在裁剪版
源码包上执行会失败——这是裁剪的必然结果。另外，覆盖率报告中 8 份被裁模块的逐行页已替换
为说明页（coverage.py 会逐行内嵌被测源码），**汇总页的文件清单与全部覆盖率数字一条未动**。
详见《源码裁剪说明.md》第五节。

## 五、使用声明

本材料仅供赛事评审使用，不授予再分发、再实施或商业使用的权利。
