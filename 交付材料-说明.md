# 交付材料说明

本仓库根目录下有三份交付物，按**部署**、**源代码**与**单元测试**分开，各自可单独审阅：

| 目录 | 交付物 | 回答什么问题 |
| --- | --- | --- |
| [`交付材料-Docker部署/`](交付材料-Docker部署/) | Docker 部署说明、源代码说明、部署文件副本 | 怎么把系统跑起来 |
| [`交付材料-源代码/`](交付材料-源代码/) | 裁剪版源码树、逐文件裁剪清单 | 代码长什么样 |
| [`交付材料-单元测试/`](交付材料-单元测试/) | 单元测试报告、pytest 输出、覆盖率报告 | 代码被测试验证到什么程度 |

> ⚠️ **源码包为裁剪版**：4 组核心算法模块（93 个文件、24,481 行）的实现已移除，保留公开
> 接口签名。裁剪范围、原因与对测试结果的影响见根目录 [`源码裁剪说明.md`](源码裁剪说明.md)。
> 完整实现保留在私有仓库，可按赛事要求向评审开放权限。

## 一、Docker 部署

```
交付材料-Docker部署/
├── README.md              最短启动路径与两个必看的坑
├── Docker部署说明.md       环境要求、compose 编排、停止清理、排障表
├── 源代码说明.md           仓库地址、分支、代码规模、目录职责、技术栈
└── deploy/                docker-compose.yml + 两个 Dockerfile + nginx.conf
```

核心结论：`docker compose up -d backend frontend` 一条命令拉起数据库、迁移、数据装载与前后端。

## 二、源代码（裁剪版）

```
交付材料-源代码/
├── README.md              包内容、规模与复现命令
├── 裁剪清单.json           逐文件裁剪清单（路径 + 原始行数）
└── src/                   裁剪后的源码树，结构与仓库一致
```

保留 230 个文件 / 59,917 行（含全部前端、API 路由层、数据层、迁移与全部单元测试）；
裁剪 93 个文件 / 24,481 行（`backend/tools/` 离线管线，以及 discovery、clustering、
extraction 三个模块的算法实现）。被裁位置保留目录、文件名与公开接口签名。

**本包用于代码审阅，不能直接运行**；运行系统请用上面的 Docker 部署包。

## 三、单元测试

```
交付材料-单元测试/
├── README.md              结论、材料索引与覆盖率门禁
├── 单元测试报告.md         测试范围、复现命令、四个挂载的必要性
├── 单元测试输出.txt        pytest 原始输出（归档证据）
└── coverage/              覆盖率 HTML 报告（本地生成，不入库）
```

核心结论：后端 **161 项单元测试全部通过，语句覆盖率 78.79%**，高于要求的 60%。

⚠️ 这两个数字是在**完整代码**上跑出来的，不是裁剪版。38 个测试文件中有 19 个依赖被裁模块，
在 `交付材料-源代码/` 上执行会失败——这是裁剪的必然结果。需要复核的评审请按
[`源码裁剪说明.md`](源码裁剪说明.md) 第六节获取完整仓库。

## 打包提交

三个目录可连同 `源码裁剪说明.md` 一并压缩提交。两处注意：

1. `交付材料-单元测试/coverage/` 是 pytest 产物、不入 git，从仓库克隆的副本里没有它——
   **打包前确认本地已生成**，或按 `单元测试报告.md` 的命令重新生成后再压缩。
2. `交付材料-源代码/src/` 由脚本生成、不入 git，打包前先跑一次
   `python scripts/build_redacted_source_package.py`。
