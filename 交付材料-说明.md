# 交付材料说明

本仓库根目录下有两份交付物，按**部署**与**测试**分开，各自完整、可单独审阅：

| 目录 | 交付物 | 回答什么问题 |
| --- | --- | --- |
| [`交付材料-Docker部署/`](交付材料-Docker部署/) | Docker 部署说明、源代码说明、部署文件副本 | 怎么把系统跑起来 |
| [`交付材料-测试/`](交付材料-测试/) | 测试代码、测试说明文档、测试结果 | 这套系统被怎么验证过、结论是什么 |

## 一、Docker 部署

```
交付材料-Docker部署/
├── README.md              最短启动路径与两个必看的坑
├── Docker部署说明.md       环境要求、compose 编排、停止清理、排障表
├── 源代码说明.md           仓库地址、分支、代码规模、目录职责、技术栈
└── deploy/                docker-compose.yml + 两个 Dockerfile + nginx.conf
```

核心结论：`docker compose up -d backend frontend` 一条命令拉起数据库、迁移、数据装载与前后端。

## 二、测试

```
交付材料-测试/
├── README.md                    两层测试的材料索引与结论
├── 单元测试报告.md               测试范围、覆盖率门禁、复现命令
├── 单元测试输出.txt              pytest 原始输出（归档证据）
├── coverage/                    覆盖率 HTML 报告（本地生成，不入库）
└── 功能测试-新岗位发现链路/
    ├── 测试报告.md / .docx       两条外部证据链路的端到端可追溯性验证
    ├── export_test_chain.py     导出脚本，口径与判定工具一致
    ├── input/ output/ db/       输入样例、候选完整记录、数据库样例
    └── screenshots/             平台呈现截图
```

核心结论：后端 **161 项单元测试全部通过，语句覆盖率 78.79%**（要求 ≥ 60%）；
新岗位发现两条链路的招聘侧基线均可用库内原始数据现场复算，与候选卡逐项吻合。

单元测试的测试代码在仓库 `backend/tests/`（38 个文件），未在交付目录里重复一份，避免与源码脱节。

## 打包提交

两个目录可连同仓库整体压缩提交。注意 `交付材料-测试/coverage/` 是 pytest 产物、不入 git，
从仓库克隆的副本里没有它——**打包前确认本地已生成，或按 `单元测试报告.md` 的命令重新生成后再压缩**。
