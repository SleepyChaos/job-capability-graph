# 提交说明

具身智能岗位能力图谱平台 · 软件模块交付材料。

## 交付项对照

| 赛题要求 | 本次提交 |
| --- | --- |
| 源代码 | [`交付材料-源代码/`](交付材料-源代码/) |
| 可执行程序 | 在线部署 <http://122.51.220.41:8080/>；源码包亦可本地 `docker compose up` 运行 |
| 部署说明（Dockerfile / 容器化部署） | [`交付材料-Docker部署/`](交付材料-Docker部署/) |
| 单元测试用例（覆盖率 ≥ 60%） | [`交付材料-单元测试/`](交付材料-单元测试/)，161 项通过、语句覆盖率 78.79% |

## 在线部署

**<http://122.51.220.41:8080/>**

系统功能已部署上线，评审可在线开放访问，无需安装任何环境。该环境运行完整代码，功能包括：

- 新岗位发现：运行自动预测生成候选卡 → 审核台逐条处置入库 → 岗位画像图谱查看呈现 → 岗位能力演变追踪
- LLM 生成候选表达层与五维能力画像
- 岗位聚类、结构化抽取、数据采集治理全链路

这是查看完整功能的推荐入口。

## 源代码

```
交付材料-源代码/
├── README.md              包内容与启动方式
├── 裁剪清单.json           未包含实现的文件清单
└── src/
    ├── docker-compose.yml
    ├── db/                运行库快照，首启自动导入
    ├── backend/           API 路由层、数据层、迁移、全部单元测试
    └── frontend/          全部前端源码与图谱产物
```

```bash
cd 交付材料-源代码/src && docker compose up -d --build
```

首次启动约 6 分钟（含数据导入与镜像构建），之后重启即时可用。启动后访问
<http://localhost:8080>。

考虑到新岗位发现推演、岗位聚类与结构化抽取属于本作品的核心方法，源码包中这几个模块
仅保留公开接口签名，未包含具体实现（93 个文件，逐条见 `裁剪清单.json`）；其余代码完整
提交。数据浏览、图谱与画像查看等功能正常可用，调用到未包含实现的操作返回 HTTP 501 并
提示访问在线部署。完整实现保留在私有仓库，可按赛事要求向评审开放访问权限。

## Docker 部署

```
交付材料-Docker部署/
├── README.md              启动入口与容器编排
├── Docker部署说明.md       环境要求、启动方式、编排细节、已知问题与排障
├── 源代码说明.md           仓库地址、分支、代码规模、目录职责、技术栈
└── deploy/                docker-compose.yml + 两个 Dockerfile + nginx.conf
```

## 单元测试

```
交付材料-单元测试/
├── README.md              结论、材料索引与覆盖率门禁
├── 单元测试报告.md         测试范围、复现命令、四个挂载的必要性
├── 单元测试输出.txt        pytest 原始输出
└── coverage/              覆盖率 HTML 报告
```

后端 **161 项单元测试全部通过，语句覆盖率 78.79%**，高于要求的 60%。阈值写死在
`pyproject.toml` 的 `fail_under = 60`，低于该线直接判失败，不依赖人工检查。

该结果在完整代码上运行得出。

## 使用声明

本材料仅供赛事评审使用，不授予再分发、再实施或商业使用的权利。
