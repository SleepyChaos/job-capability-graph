# Docker Compose 使用：复制为仓库根目录 `.env`，再填写 DeepSeek Key。
# `.env` 已被 .gitignore 排除，禁止提交真实密钥。
APP_LLM_API_KEY=
APP_LLM_BASE_URL=https://api.deepseek.com/v1
APP_LLM_MODEL=deepseek-v4-flash
APP_LLM_TIMEOUT_SECONDS=30

# 生产部署前必须替换。
APP_AUTH_SECRET=change-me-in-production
