# 交付材料 · 源代码（裁剪版）

对应赛题「软件模块」交付项：**源代码**。部署材料在 `交付材料-Docker部署/`，测试材料在
`交付材料-单元测试/`。

## ⚠️ 本包是裁剪版

4 组核心算法模块（93 个文件、24,481 行）的实现已移除，保留目录、文件名与公开接口签名。
**裁剪范围、原因与影响见根目录 [`源码裁剪说明.md`](../源码裁剪说明.md)——审阅本包前请先读那份。**

本包用于**代码审阅**，不能直接运行；需要运行系统请用 `交付材料-Docker部署/`。

## 内容

| 路径 | 说明 |
| --- | --- |
| [`src/`](src/) | 裁剪后的源码树，结构与仓库一致 |
| [`裁剪清单.json`](裁剪清单.json) | 逐文件裁剪清单（路径 + 原始行数），机器可读 |

```
src/
├── backend/
│   ├── app/
│   │   ├── api/          FastAPI 路由层            保留
│   │   ├── modules/      领域模块                  部分裁剪，见说明
│   │   ├── core/ db/     配置、会话与引擎          保留
│   ├── migrations/       Alembic 迁移              保留
│   ├── tools/            离线管线                  已裁剪（82 个文件）
│   └── tests/            单元测试                  保留（38 个文件）
├── frontend/
│   ├── src/              页面、图谱组件、接口客户端  保留
│   └── public/           图谱静态产物              超 1MB 的三份未入包
└── docker-compose.yml
```

## 规模

| 项 | 文件数 | 行数 |
| --- | ---: | ---: |
| 保留 | 230 | 59,917 |
| 裁剪 | 93 | 24,481 |

## 复现

```bash
python scripts/build_redacted_source_package.py
```
